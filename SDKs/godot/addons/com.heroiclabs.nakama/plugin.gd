@tool
extends EditorPlugin

## Minimal editor plugin so "Nakama" appears in Project Settings → Plugins.
## The addon is a library; IntelliVerseX loads it at runtime. This only adds the checkbox.

func _enter_tree() -> void:
	pass

func _exit_tree() -> void:
	pass
