# tools/

| Folder | Role |
|--------|------|
| `boilerplate/` | Large multi-engine templates (~171 MB). **Not** part of `com.intelliversex.sdk`. Gitignored (P1). Keep locally if you need generators; do not copy into the UPM package. |
| Other folders | Internal scripts / QA — repo tooling only |

Sandbox Unity project: `SDKs/unity/editor` (vendors: Photon, Nakama, ads SDKs).  
Shippable package: `Packages/com.intelliversex.sdk`.
