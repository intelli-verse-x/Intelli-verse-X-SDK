name: Flutter SDK

on:
  push:
    paths:
      - 'SDKs/flutter/**'
  pull_request:
    paths:
      - 'SDKs/flutter/**'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # nakama ^1.3.0 → protobuf ≥4.1.0 requires Dart SDK ≥3.6
        sdk: ['3.6.0', 'stable']

    steps:
      - uses: actions/checkout@v4

      - name: Setup Dart ${{ matrix.sdk }}
        uses: dart-lang/setup-dart@v1
        with:
          sdk: ${{ matrix.sdk }}

      - name: Install dependencies
        working-directory: SDKs/flutter
        run: dart pub get

      - name: Analyze
        working-directory: SDKs/flutter
        run: dart analyze --fatal-infos

      - name: Test
        working-directory: SDKs/flutter
        run: dart test
